#include<ctime>
#include<iostream>

int main(int argc, char**argv)
{
    int i,j,k;
    int loop;

    loop = atoi(argv[1]);
    
    int len = 10000000;
    double *arr = new double[len]{};
    double temp;

    for(i=0; i<loop; i++)
    {
        for(j=0;j<len;j++)
        {
            
        }
    }


    return 0;
}